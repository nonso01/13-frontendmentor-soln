import Button from "./Button";
import Input from "./Input";

import { useState } from "react";

function Form({}) {
	return (
		<div className="form">
		<form> 
		</form>
		</div>
	);
}

export default Form;
